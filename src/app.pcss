/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

.btn {
  @apply disabled:text-opacity-60;
}

.btn-disabled {
  @apply !text-opacity-60;
}
